import React from "react";
import Container from "@mui/material/Container";

const SFIDCapture = (props) => {
  return <Container maxWidth="xl" id="camera_view"></Container>;
};

export default SFIDCapture;
